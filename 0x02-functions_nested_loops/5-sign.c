#include <stdio.h>
/**
*main - Prints the sign of the number
*
*Return : Always 0 (success)
*/

int main(void)
{
int print_sign(void);
int n;
	if (n > 0)
	{
		putchar('0');
		return (1);
	}
	else if (n == 0)
	{
		putchar(0);
		return (0);
	}
	else
	{
		putchar('-');
		return (-1);
	}
}
