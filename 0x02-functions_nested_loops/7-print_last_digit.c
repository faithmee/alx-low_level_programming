#include <stdio.h>
/**
 * main - check the code
 * Return: Always 0.
 */
int main(void)
{
	int print_last_digit(int h);
	{
		int h = 0;
		int ld = h;

		if (ld < 0)
			ld *= -1;
		putchar(ld + '0');
		return (0);
	}
}

