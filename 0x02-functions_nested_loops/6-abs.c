#include <stdio.h>

/**
 *main - Check the code
 * _abs - Computs the absolute value of an integer
 *@t: The integer to be computed
 *Return: The absolute value
 */
int main(void)

	int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}

