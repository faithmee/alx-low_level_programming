#include "main.h"
/**
 * main - Entry point
 *
 * _puts - checks the code
 * @*str: pointer to the code
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	_puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
