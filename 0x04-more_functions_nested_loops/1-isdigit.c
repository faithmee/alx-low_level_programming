#include "main.h"
/**
 *main - Check the code
 *description: Write a function that checks for a digit (0 through 9)
 *_isdigit - Check if numbers are 0 - 9
 *Return: 0 or 1
 */
int main(void)
{
	int _isdigit(int x);
	{
	if (x >= '48' && x <= '57')
	{
		return (1);
	}
	return (0);
	}
}
